{
  "name": "ishaqzada-delivery",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ishaqzada-delivery-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}